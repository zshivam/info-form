[project]
name = "info-form-api"
version = "0.1.0"
description = "FastAPI & Vue 3 application on Vercel"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.0.0",
    "pymongo>=4.8.0",
    "python-multipart>=0.0.9",
    "python-dotenv>=1.0.0"
]

[tool.vercel.fastapi.static]
cdn = false
